# Usado ao rodar a aplicação fora do Docker (bun dev direto)
# Para rodar via Docker, use: docker-compose up

# Banco local (requer o serviço db do docker-compose rodando)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/spend_rules"

# Auth
AUTH_SECRET="your-secret-here"
AUTH_URL="http://localhost:3000"
